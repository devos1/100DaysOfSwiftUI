import UIKit

let first = 11
let second = 12

print(first == second ? "Yes, the same" : "No, not the same")

// regular method
if first == second {
    print("Yes, the same")
}else{
    print("No, not the same")
}
